my-app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 600s
  }
  internal {
    # If ask takes more time than this to complete the call is failed
    ask-timeout = 600s
  }
  game {
    max-players-per-game = 6
  }
}
